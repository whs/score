# Frontend

## Development

- Start dev server: `npm dev`
- Production build: `npm build` and use the dist directory

Before committing:

- If new text is added, run `npm run locale-extract`, update the xliff files, then `npm run locale-build` before committing
- Run `npm run prettier`
